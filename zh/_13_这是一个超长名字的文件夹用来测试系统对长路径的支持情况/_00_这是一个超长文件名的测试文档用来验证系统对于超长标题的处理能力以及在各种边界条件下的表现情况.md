---
title: VMD 完整功能测试
created_at: @git
last_updated_at: @git
author: @git
tags: [tutorial, test]
has_custom_tsx: false
---

# VMD 完整功能测试

本文档演示所有 VMD 功能，包含适当的嵌套层级。

## 块级数学公式

以下是常见的数学公式（代码中不含中文）：

$$
\frac{d}{dx} e^x = e^x
$$

$$
H\Psi = E\Psi
$$


## 超大号测试表格

此表格包含大量行内代码与行内数学公式，用于测试大规模渲染表现。

<table>

| 特性 | 语法 | LaTeX 行内公式 | Python 代码 | JS/TS 代码 | 结果 | 复杂度 | 备注 |
|---|---|---|---|---|---|---|---|
| 行内数学 | Invalid | $\nabla^2 \phi = 4\pi G \rho$ | `typing.NewType("UserId", int)` | `useState(0)` | ERR | O(n!) | 行 1 |
| 块级数学 | Experimental | $\mathbf{J} = \sigma \mathbf{E}$ | `pip install numpy pandas` | `useEffect(() => {}, [])` | ERR | O(n) | 行 2 |
| 代码块 | Experimental | $\mathfrak{sl}(2, \mathbb{C})$ | `typing.NewType("UserId", int)` | `export default function App() {}` | OK | O(2ⁿ) | 行 3 |
| 行内代码 | Partial | $\sigma^2 = \frac{1}{N}\sum (x_i - \mu)^2$ | `def foo(x): return x**2` | `useState(0)` | ERR | O(n log n) | 行 4 |
| 表格单元格 | Invalid | $\sum_{i=1}^{n} x_i^2$ | `class Model(nn.Module): pass` | `structuredClone(obj)` | DEBUG | O(1) | 行 5 |
| 嵌套表达式 | Valid | $\mathrm{Var}(X) = \mathbb{E}[X^2] - (\mathbb{E}[X])^2$ | `map(str, range(10))` | `Date.now() - startTime` | OK | O(2ⁿ) | 行 6 |
| 转义字符 | Invalid | $\lim_{x \to 0} \frac{\sin x}{x} = 1$ | `enumerate(["a", "b", "c"])` | `localStorage.getItem("key")` | DEBUG | O(n) | 行 7 |
| Unicode 混合 | Partial | $\nabla^2 \phi = 4\pi G \rho$ | `functools.reduce(operator.add, xs)` | `const debounced = useDebounce(value, 300)` | INFO | O(n) | 行 8 |
| 长公式 | Experimental | $\mathfrak{sl}(2, \mathbb{C})$ | `dict.get(key, default)` | `interface Props { children: ReactNode }` | WARN | O(n!) | 行 9 |
| 短引用 | Partial | $\oint_C \mathbf{F} \cdot d\mathbf{r}$ | `enumerate(["a", "b", "c"])` | `return <div className="flex" />` | ERR | O(n²) | 行 10 |
| 矩阵符号 | Experimental | $\ell^p = \{x : \sum |x_i|^p < \infty\}$ | `df.groupby(["col"]).sum()` | `Object.keys(data).filter(Boolean)` | WARN | O(1) | 行 11 |
| 集合论 | Partial | $\alpha + \beta = \gamma$ | `import matplotlib.pyplot as plt` | `import { useRouter } from "next/router"` | WARN | O(2ⁿ) | 行 12 |
| 微积分 | Invalid | $\alpha + \beta = \gamma$ | `np.linalg.norm(v)` | `interface Props { children: ReactNode }` | OK | O(n) | 行 13 |
| 代数学 | Valid | $\lfloor x \rfloor \leq x < \lceil x \rceil$ | `lambda x: x + 1` | `Intl.NumberFormat("en").format(n)` | ERR | O(1) | 行 14 |
| 统计学 | Experimental | $\Gamma(z) = \int_0^\infty t^{z-1}e^{-t}dt$ | `await asyncio.sleep(1)` | `type User = { id: string; name: string }` | INFO | O(n) | 行 15 |
| 概率论 | Experimental | $\lim_{x \to 0} \frac{\sin x}{x} = 1$ | `dict.get(key, default)` | `crypto.randomUUID()` | DEBUG | O(2ⁿ) | 行 16 |
| 逻辑门 | Deprecated | $\Gamma(z) = \int_0^\infty t^{z-1}e^{-t}dt$ | `list comprehension [x for x in xs]` | `localStorage.getItem("key")` | INFO | O(n) | 行 17 |
| 类型论 | Valid | $\frac{\partial f}{\partial x}$ | `enumerate(["a", "b", "c"])` | `document.querySelector("#app")` | ERR | O(n²) | 行 18 |
| 范畴论 | Deprecated | $\ell^p = \{x : \sum |x_i|^p < \infty\}$ | `functools.reduce(operator.add, xs)` | `const memoized = useMemo(() => compute(x), [x])` | OK | O(n!) | 行 19 |
| 数论 | Valid | $\sum_{i=1}^{n} x_i^2$ | `json.dumps(data, indent=2)` | `const memoized = useMemo(() => compute(x), [x])` | ERR | O(1) | 行 20 |
| 图论 | Invalid | $\hat{H}\psi = E\psi$ | `pip install numpy pandas` | `import { useRouter } from "next/router"` | INFO | O(n) | 行 21 |
| 拓扑学 | Deprecated | $\nabla \cdot \mathbf{E} = \frac{\rho}{\varepsilon_0}$ | `await asyncio.sleep(1)` | `new URLSearchParams(window.location.search)` | WARN | O(1) | 行 22 |
| 群论 | Deprecated | $\mathsf{H} \in \mathrm{Mat}_{n \times n}$ | `functools.reduce(operator.add, xs)` | `Intl.NumberFormat("en").format(n)` | DEBUG | O(1) | 行 23 |
| 环论 | Valid | $\frac{d^2x}{dt^2} + \omega^2 x = 0$ | `sorted(xs, key=lambda x: -x)` | `Date.now() - startTime` | OK | O(1) | 行 24 |
| 域论 | Invalid | $\nabla \cdot \mathbf{E} = \frac{\rho}{\varepsilon_0}$ | `with open(path) as f: pass` | `localStorage.getItem("key")` | INFO | O(n) | 行 25 |
| 线性代数 | Deprecated | $\lfloor x \rfloor \leq x < \lceil x \rceil$ | `lambda x: x + 1` | `npm install react react-dom` | INFO | O(1) | 行 26 |
| 微分方程 | Deprecated | $\mathbb{R}^n \to \mathbb{C}$ | `typing.NewType("UserId", int)` | `Math.max(...arr)` | INFO | O(n log n) | 行 27 |
| 积分变换 | Deprecated | $\wp(z; \omega_1, \omega_2)$ | `dict.get(key, default)` | `Math.max(...arr)` | DEBUG | O(n!) | 行 28 |
| 级数展开 | Deprecated | $\lim_{x \to 0} \frac{\sin x}{x} = 1$ | `pip install numpy pandas` | `Promise.all([p1, p2])` | WARN | O(1) | 行 29 |
| 张量符号 | Experimental | $\mathrm{Var}(X) = \mathbb{E}[X^2] - (\mathbb{E}[X])^2$ | `sorted(xs, key=lambda x: -x)` | `arr.map(x => x * 2)` | ERR | O(1) | 行 30 |

</table>
