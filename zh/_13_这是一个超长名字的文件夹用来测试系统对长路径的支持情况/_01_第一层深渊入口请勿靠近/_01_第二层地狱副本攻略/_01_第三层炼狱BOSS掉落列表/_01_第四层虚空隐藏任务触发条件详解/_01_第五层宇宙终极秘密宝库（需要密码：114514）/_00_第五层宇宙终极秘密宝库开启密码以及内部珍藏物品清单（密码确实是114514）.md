---
title: 层级五大师级
created_at: @git
last_updated_at: @git
author: @git
tags: [final]
has_custom_tsx: false
---

# 层级五大师级

达到最大深度！五层嵌套。

路径：`zh/_13_完整测试/_01_嵌套层级一/_01_嵌套层级二/_01_嵌套层级三/_01_嵌套层级四/_01_嵌套层级五/`

## 大师方程

爱因斯坦场方程：

$$
P(A|B) = \frac{P(B|A)P(A)}{P(B)}
$$

## 量子计算

```python
from qiskit import QuantumCircuit, execute, Aer

# Create a quantum circuit
def create_bell_state():
    """Create a Bell state."""
    qc = QuantumCircuit(2, 2)
    qc.h(0)
    qc.cx(0, 1)
    qc.measure([0, 1], [0, 1])
    return qc

# Execute
simulator = Aer.get_backend('qasm_simulator')
job = execute(create_bell_state(), simulator)
result = job.result()
print(result.get_counts())
```

## 最深层的 Post 组件

<post>
<lft>

此 post 组件位于最深的嵌套层级。它展示了 VMD post 组件在任意文件夹深度都能正常工作。

展示的功能：
- 深度嵌套支持
- 任意层级的图片渲染
- 数学公式：$f'(x) = \lim_{h \to 0} \frac{f(x+h)-f(x)}{h}$

</lft>
<rt>

![深层图片 1](/assets/images/img_01.jpg)
![深层图片 2](/assets/images/img_02.jpg)

</rt>
</post>

## 恭喜

你已到达测试文档结构的最深层！
